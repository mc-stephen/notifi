[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
notification_core = { path = "../notification/core" }
email_channel = { path = "../notification/channels/email_channel" }
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }


