1. Billing should be par organization and not par account
2. Users can't create more than 10 orgs without emailing support. this is to avoid abuse of the system.
3. On the current dashboard, there is orgs and project on the header side. We only need one which will be **orgs**
4. on the current project on the side bar, remove the **project** section it is not needed, to create a new project, the dropdown on the header bar is okay (we just have to add the btn for that)
5. The notification icon on the header section does notting, so we will have to implement it logic, which will show a small floating notitcation dialog.
6. in the "Channel" section, the "provider" page will be for adding providers like adding 3 provider for emailing, and then the "channel" page will be for configuring a channel (eg email) and then selecting one of the pre-config providers
7. On the Onboarding page, the last Step (Success Step), the btn that says "Read the Docs" the link should come from the dashboard project env, and not to be had coded.
